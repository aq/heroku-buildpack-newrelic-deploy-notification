#!/usr/bin/env bash

if [ "empty$NEW_RELIC_API_KEY"  == "empty" || "empty$NEW_RELIC_APP_ID" == "empty" ]; then
  echo "no NEW_RELIC_API_KEY or no NEW_RELIC_APP_ID"
  exit 1
else
  echo "Notify NewRelic"
  exit 0
fi
